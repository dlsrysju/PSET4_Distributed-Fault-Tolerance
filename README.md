# PSET4_Distributed-Fault-Tolerance

for auth-controller-node,
- npm install express cors dotenv bcryptjs jsonwebtoken @grpc/grpc-js @grpc/proto-loader

for course and grade controller node,
- npm install express cors dotenv @grpc/grpc-js @grpc/proto-loader

for view node,
- npm install node-fetch@2  
- npm install express cors dotenv @grpc/grpc-js @grpc/proto-loader